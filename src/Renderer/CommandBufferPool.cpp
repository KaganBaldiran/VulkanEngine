#include "CommandBufferPool.hpp"

RENDERER::CommandBufferPool::CommandBufferPool(VkDevice LogicalDevice)
{

}

void RENDERER::CommandBufferPool::Create(VkDevice LogicalDevice)
{

}

void RENDERER::CommandBufferPool::Destroy(VkDevice LogicalDevice)
{
	//RENDERER_CORE::AllocateCommandBuffers(RendererContextPtr->CommandPool.Handle, LogicalDevice, CommandBuffers);

}